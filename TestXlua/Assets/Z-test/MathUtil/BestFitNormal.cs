using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace MathTools
{
    public class BestFitNormal
    {
        public Vector3 ComputeBest(Vector3[] pos, int n)
        {
            Vector3 temp = MathUtil.kZeroVector;
            for (int i = 0; i < pos.Length - 1; i++)
            {
                Vector3 v = pos[i];
            }
            return temp;
        }
    }
}
